[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "allora-forge-builder-kit"
version = "3.0.0"
description = "Allora ML Workflow Utilities — build, evaluate, and deploy models to the Allora Network"
authors = [{ name = "Tim DeLise", email = "tim@alloralabs.xyz" }]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pandas",
    "numpy",
    "lightgbm",
    # lightgbm's sklearn API (LGBMRegressor, used by workflow.py) imports
    # scikit-learn at runtime; without it training dies with "scikit-learn is
    # required for lightgbm.sklearn". It is a hard runtime dep, not dev-only.
    "scikit-learn",
    "requests",
    "dill",
    "polars",
    "pyarrow",
    "pytz",
    "numba",
    "scipy",
    "allora-sdk>=1.3.0",
    "cloudpickle",
    "websocket-client",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "websocket-client",
    "scikit-learn",
    "matplotlib",
]
wallet-link = [
    "cosmpy>=0.11.1",
]


[project.scripts]
workerctl = "allora_forge_builder_kit.workerctl:main"
allora-forge-link = "allora_forge_builder_kit.wallet_link:main"

[project.urls]
Homepage = "https://github.com/allora-network/allora-forge-builder-kit"

[tool.setuptools.packages.find]
include = ["allora_forge_builder_kit"]

