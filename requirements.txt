# allora-sdk v1.1.0 cannot be installed via pip — the generated gRPC protos
# are not committed to the repo. Install it manually before this file:
#
#   git clone https://github.com/allora-network/allora-sdk-py.git
#   cd allora-sdk-py && git checkout 8ed8ebf
#   pip install ".[codegen]" && make codegen && pip install -e .
#
# Then install the builder kit:
#   pip install -r requirements.txt
#   pip install -e ".[dev,wallet-link]"

lightgbm
scikit-learn
scipy
pandas
numpy
matplotlib
cloudpickle
websocket-client
cosmpy==0.11.1
