# PR #36 — ENGN-8456: Identity & Wallet Foundation (builder-kit)

**Branch:** `tani/ENGN-8456-epic-identity-and-wallet-foundation`
**PR URL:** https://github.com/allora-network/allora-forge-builder-kit/pull/36
**Suite:** 166 passed, 20 skipped, 0 failed (as of 2026-08-12)

---

## Review Threads — Complete Status

All threads posted 2026-06-29 by cubic (automated reviewer). "Resolved" = closed on GitHub.

| ID | Sev | File | Finding | Status |
|---|---|---|---|---|
| synth-028 | HIGH | wallet_link.py | `HTTPSConnection` to HTTPS CONNECT proxies | **RESOLVED** — false positive, stdlib pattern |
| synth-029 | HIGH | wallet_link.py | `workerctl link` ignores `$FORGE_BACKEND_URL` | **RESOLVED** — fixed, argparse default reads env |
| synth-030 | MED | worker_manager.py | `_load_secrets` silently returns `{}` on corrupt file | **RESOLVED** — fixed, `FileNotFoundError`→`{}`, others re-raise |
| synth-031 | MED | worker_manager.py | `address` unsanitized in filesystem paths | **RESOLVED** — fixed, `_assert_safe_address()` added |
| synth-032 | MED | worker_manager.py | `_validate_artifact_for_deploy` swallows every Exception | **RESOLVED** — fixed, re-raises `OSError` |
| synth-033 | MED | worker_manager.py | `ALLORA_API_KEY` presence check permanently cached | **RESOLVED** — fixed, cache removed, re-reads on every call |
| synth-034 | MED | worker_manager.py | Imports `ForgeBackendClient` from internal SDK path | **RESOLVED** — fixed by d9623e7, uses `allora_sdk.ForgeBackendClient` with try/except fallback |
| cubic-P3 | LOW | wallet_link.py | `discover_keys` raises `UnicodeDecodeError` on binary file | **RESOLVED** — fixed, added to except clause |
| cubic-new | INFO | worker_manager.py | `from allora_sdk import ForgeBackendClient` will fail on PyPI | **RESOLVED** — same root as synth-034, fixed by d9623e7 |
| cubic-P2 | LOW | worker_manager.py | `acquire(blocking=False)` hard 8-ceiling even on healthy backends | **OPEN** — needs decision |
| synth-035 | MED | worker_manager.py | Managed replace reads artifact 3× (validate + sha256 + copy2) | **OPEN** — needs decision |
| synth-036 | MED | worker_manager.py | `_create_deployment_record` re-hashes on every new-worker deploy | **OPEN** — needs decision |
| synth-037 | MED | both | Inline comment density violates style guide | **OPEN** — partially addressed by 679ef3a; resolve as addressed? |
| synth-038 | LOW | wallet_link.py | `run_link` crashes (TypeError) when server returns `challenges: null` | **OPEN** — real bug, one-line fix |
| synth-039 | LOW | worker_manager.py | `key_dir`/`runtime_log_dir` created with default 0o755 | **OPEN** — security |
| synth-040 | LOW | worker_manager.py | Semaphore saturation has no operator-visible counter | **OPEN** — needs decision |
| synth-041 | LOW | wallet_link.py | Mnemonic stays in frame for full 30-min poll loop | **OPEN** — security |
| synth-042 | LOW | worker_manager.py | `chmod` failure on pre-existing log silently swallowed | **OPEN** — needs decision |
| synth-043 | LOW | wallet_link.py + workerctl.py | CLI arg surface declared twice with no shared builder | **OPEN** — architecture |
| synth-044 | LOW | wallet_link.py | `_select_proxy` uses `print` instead of `warnings.warn` | **OPEN** — style; wallet_link is CLI not library |
| synth-045 | LOW | wallet_link.py | `_submit_rejection` uses manual loop instead of comprehension | **OPEN** — style only |
| synth-046 | LOW | worker_manager.py | `_validate_artifact_for_deploy` rebinds `found` each iteration | **OPEN** — style only |
| synth-047 | LOW | worker_manager.py | `status_worker`/`status_all` return unparametrized `dict` | **OPEN** — typing |
| synth-048 | LOW | worker_manager.py | `reconcile()` makes N+1 SQLite connections | **OPEN** — performance |
| synth-049 | LOW | wallet_link.py | `_submit_rejection` parses response shape server never emits | **OPEN** — cross-repo, defensive code |
| synth-050 | INFO | wallet_link.py | Duplicate `--address` flags produce duplicate signatures | **OPEN** — real bug, one-line fix |
| synth-051 | INFO | worker_manager.py | `_deploy_managed_worker` validates wallet info with truthiness only | **OPEN** — minor |
| synth-052 | INFO | both | `@@TODO` markers not recognized by IDE/CI tooling | **OPEN** — style, quick fix |
| synth-053 | INFO | wallet_link.py | `wallet_link` bypasses `ForgeClientProtocol` seam | **OPEN** — architecture debt, documented |

---

## Action Queue

### Fix now — real bugs
- [ ] **synth-038** — `start.get("challenges") or []` prevents TypeError when server sends `null`
- [ ] **synth-050** — deduplicate `selected` addresses before building signatures

### Fix now — quick/unambiguous
- [ ] **synth-052** — replace `@@TODO` with `# TODO` throughout
- [ ] **synth-039** — `mkdir(mode=0o700)` for `key_dir`, `runtime_log_dir`, `artifact_dir`

### Resolve with note — already addressed in prior commits
- [ ] **synth-034 / cubic-new** — resolve together; fixed by d9623e7

### Needs human decision
- [ ] **cubic-P2** — semaphore hard ceiling applies even to healthy backends; keep or use a counting approach?
- [ ] **synth-035** — 3× artifact reads on replace; combine validate+sha256 into one pass?
- [ ] **synth-036** — re-hash on new deploy; pass computed hash through to `_create_deployment_record`?
- [ ] **synth-040** — semaphore saturation not visible; add a blocked-total counter?
- [ ] **synth-041** — mnemonic in frame 30 min; `del mnemonic` after signing? (CPython caveat)
- [ ] **synth-042** — chmod failure bare `pass`; log a warning instead?
- [ ] **synth-043** — CLI args in two places; extract shared argparse builder?
- [ ] **synth-048** — N+1 SQLite in `reconcile()`; pass status rows into `start_worker`?
- [ ] **synth-049** — dead `_submit_rejection` server-shape parser; remove or keep as defensive?
- [ ] **synth-051** — add `isinstance(str)` check on wallet address/id?
- [ ] **synth-053** — document architecture debt or refactor into `ForgeClientProtocol`?

### Won't-fix — resolve with note
- [ ] **synth-037** — inline comments already trimmed by 679ef3a; style is subjective
- [ ] **synth-044** — `print` to stderr is standard for CLI tools; `warnings.warn` is for libraries
- [ ] **synth-045** — manual loop in `_submit_rejection`; no bug, cosmetic only
- [ ] **synth-046** — `found` rebinding; no bug, list allocation negligible at 64 KiB chunks
- [ ] **synth-047** — unparametrized `dict`; `TypedDict` out of scope for this PR
- [ ] **synth-053** — architecture debt already documented via `@@TODO` comment; leave for follow-up

---

## Test Suite
- `uv run pytest` → **166 passed, 20 skipped, 0 failed**
- 20 skipped: 14 integration-gated (live Allora node), 3 pending allora-sdk-py#83, 3 SDK contract tests
